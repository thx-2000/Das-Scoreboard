# Direktzugriff auf PHP-Includes verbieten (Apache 2.4 + Fallback 2.2)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
